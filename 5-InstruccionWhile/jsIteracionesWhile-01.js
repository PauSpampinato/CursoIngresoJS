/*
al presionar el botón mostrar 10 repeticiones 
con números ASCENDENTE, desde el 1 al 10.*/
function mostrar() {
  let i;
  i = 0;
  while (i < 10) {
    i++;
    console.log(i);
  }

  /*let numero; //se declara la variable
  numero = 1; // se inicializa la variable
  while (numero <= 10) { //variable de control (condición)
    alert("el numero es: " + numero);
    numero = numero + 1; //modificación de la variable de control (también
    //se puede utilizar i++ o i-- para sumar o restar 1)
  }*/
} //FIN DE LA FUNCIÓN
